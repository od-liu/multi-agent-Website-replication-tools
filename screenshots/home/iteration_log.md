# UI 调整迭代日志

## Iteration 1（首页主内容：车票查询卡片）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_1.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **缺失元素**：车票查询卡片左侧蓝色栏的图标在目标图中存在（车票/常用查询/订餐），复刻图中缺失（仅文字） - *顶部主内容 / 查询卡片左侧* - **缺失元素**
  2. **缺失元素/样式**：顶部 Tab（单程/往返/中转换乘/退改签）在目标图中每个 tab 左侧都有小图标，复刻图中缺失 - *顶部主内容 / 查询卡片顶部 tabs* - **缺失元素**
  3. **文本不一致**：目标图输入标签为“出发地 / 到达地”，复刻图为“出发城市 / 到达城市” - *顶部主内容 / 查询卡片表单* - **其他（文本）**
  4. **文本不一致**：目标图输入框 placeholder 为“简拼/全拼/汉字”，复刻图为“请选择城市” - *顶部主内容 / 查询卡片表单* - **其他（文本）**
  5. **控件样式不一致**：目标图“切换”是位于出发地/到达地右侧的小蓝色图标按钮；复刻图是居中的圆形“⇄”按钮 - *顶部主内容 / 出发地与到达地之间* - **位置/尺寸**
  6. **文本/格式不一致**：目标图日期为 `YYYY-MM-DD`（如 2026-01-17），复刻图为“1月17日 周六” - *顶部主内容 / 出发日期输入框* - **其他（格式）**
  7. **缺失元素**：目标图卡片右侧有一个小橙色圆形“收起”提示点，复刻图中无 - *顶部主内容 / 查询卡片右侧边缘* - **缺失元素**

- **从目标参考图/原站提取的信息（用于修复）**：
  - **标签文本**：`出发地` / `到达地`
  - **placeholder**：`简拼/全拼/汉字`
  - **日期展示**：`YYYY-MM-DD`
  - **切换按钮**：小图标按钮（文字 aria/tooltip 表现为“切换”）

- **修复内容**：
  - **修改文件**：
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.tsx`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1/#2：为左侧栏与 tabs 增加图标渲染（使用 mask + inline SVG，颜色跟随 currentColor）
    - #3/#4：将“出发城市/到达城市”改为“出发地/到达地”，placeholder 改为“简拼/全拼/汉字”
    - #5：将交换按钮改为右侧小图标按钮，aria-label 设为“切换”
    - #6：日期默认值改为 `YYYY-MM-DD`（示例：2026-01-17）
    - #7：补充卡片右侧小橙色“收起”圆点（装饰）

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_1_verify.png`
  - **结论**：查询卡片的核心文字/控件形态已更接近目标图；剩余差异主要集中在轮播背景内容（按要求忽略）与细微间距/对齐（下一轮继续微调）

- **剩余问题（下一轮处理）**：
  - 查询卡片整体在轮播区域中的精确位置/宽高/阴影强度仍可能与目标图存在细微偏差（需要继续像素级对齐）

## Iteration 2（首页主内容：车票查询卡片 + 右侧悬浮工具条）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_2.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **缺失元素**：目标图右侧有“最新发布/联系客服/APP下载/关闭”悬浮工具条，复刻图缺失 - *轮播区域右侧* - **缺失元素**
  2. **样式不一致（冲突导致）**：复刻图车票查询表单内“出发地/到达地/出发日期”标签出现绿色底色徽标样式，目标图为普通文字标签 - *查询卡片表单区域* - **颜色/样式**
  3. **尺寸不一致**：目标图“查 询”按钮为窄条形（约 360×30，圆角约 6px），复刻图按钮更高更粗、字距更大 - *查询卡片底部按钮* - **位置/尺寸**
  4. **样式不一致**：目标图“切换”按钮为右侧小橙色圆形图标（橙底+白边），复刻图为普通按钮形态 - *出发地/到达地右侧* - **样式**
  5. **尺寸/圆角不一致**：目标图左侧蓝色栏每项高度约 115px 且选中项为白底蓝字；复刻图间距/圆角/尺寸略偏 - *查询卡片左侧蓝色栏* - **位置/尺寸**

- **从原站提取的信息（用于修复）**：
  - **右侧悬浮工具条**（来自原站 DOM/计算样式）：
    - 宽度 `50px`；每项高度 `86px`（关闭项 `68px`）；背景 `#EFEFEF`
    - 文字颜色 `#666666`；字体 `14px`
    - 纵向间距：相邻项间隔约 `5px`
  - **“查 询”按钮**：`360×30`、`border-radius: 6px`、背景/边框 `#FF8000`、字体 `14px`
  - **“切换”图标按钮**：`24×24`、背景 `#FF8000`、白色边框 `2px`、圆形（`border-radius: 100%`）

- **修复内容**：
  - **修改文件**：
    - `frontend/src/pages/HomePage.tsx`
    - `frontend/src/pages/HomePage.css`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1：新增右侧悬浮工具条（fixed 定位，尺寸/颜色按原站测量值）
    - #2：修复全局样式冲突（`TrainList.css` 的 `.station-label` 影响查询卡片），通过更高特异性选择器覆盖为透明背景
    - #3：将“查 询”按钮调整为 `360×30`、圆角 `6px`、字体 `14px`、去除过大字距
    - #4：将“切换”按钮调整为橙色圆形图标按钮（含 2px 白边）
    - #5：将左侧蓝色栏设为固定宽度 `100px`、每项高度约 `115px`，并移除额外圆角

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_2_verify.png`
  - **结论**：右侧悬浮工具条已补齐；查询卡片的按钮尺寸/颜色与“切换”控件更接近目标图；轮播背景内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - 查询卡片内部 tabs/输入区域的精确对齐（间距、分隔线、图标细节）仍有细微偏差，可继续像素级微调

## Iteration 3（首页主内容：轮播背景区高度 + 车票查询表单布局/尺寸）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_3.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异
  - **本轮重点**：背景（轮播区域尺寸）与表单（查询卡片）

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **位置/尺寸**：轮播背景区高度在目标图更“矮”（更贴近 450px），复刻图更“高”，导致下方内容整体下移 - *轮播背景区* - **位置/尺寸**
  2. **位置/尺寸**：查询卡片在轮播区中的水平位置偏左/偏右不一致（目标图卡片左边距更大，且更贴近导航栏下方） - *轮播区左侧 / 查询卡片整体* - **位置/尺寸**
  3. **布局不一致**：目标图“出发地/到达地/出发日期”为“左侧标签 + 右侧输入框”的水平排列；复刻图为“标签在上、输入在下”的纵向排列，导致卡片高度明显偏大 - *查询卡片表单区域* - **布局**
  4. **尺寸不一致**：目标图城市输入框与日期输入框尺寸为 `265×30`，复刻图输入框更小且更高（受样式覆盖影响） - *查询卡片表单输入框* - **位置/尺寸**
  5. **样式不一致（冲突导致）**：复刻图输入框出现 `36px` 高、`border-radius: 4px` 的“通用表单样式”，与目标图 `border-radius: 0` 的直角输入不一致 - *查询卡片表单输入框* - **颜色/样式**

- **从原站提取的信息（用于修复）**：
  - **轮播背景区**：高度约 `450px`
  - **查询卡片**：`510×350`，位置约 `x=161, y=170`
  - **标签**：宽 `75px`，高度 `30px`，右侧 padding `10px`
  - **城市输入**：`265×30`，padding `4px 30px 4px 10px`，border `1px solid #DEDEDE`，`border-radius: 0`
  - **日期输入**：`265×30`，padding `4px 10px`，border `1px solid #DEDEDE`，`border-radius: 0`

- **修复内容**：
  - **修改文件**：
    - `frontend/src/pages/HomePage.css`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `frontend/src/components/TrainSearchBar/TrainSearchBar.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1：将 `.home-search-section` 的轮播背景区高度调整为更接近目标图（`min-height: 450px`），并调整 padding 使卡片更贴近目标位置
    - #2：将查询卡片整体定位到与原站一致的水平偏移（卡片 `x≈161`）
    - #3：将查询卡片表单区域改为“标签+输入框”水平布局，显著降低卡片高度
    - #4/#5：修复样式串扰（`TrainSearchBar.css` 的 `.station-input input` 覆盖了首页表单），通过为 TrainSearchBar 的内部选择器增加 `.train-search-bar` 前缀，避免影响首页；并按原站测量值重设输入框尺寸/边框/圆角

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_3_verify.png`
  - **结论**：轮播区高度、查询卡片整体位置、标签/输入布局与输入尺寸已与目标图/原站测量值基本一致；轮播背景图片内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - 查询卡片顶部 tabs（间距/下划线位置）与“切换/下拉/日历”图标的视觉细节仍可能有少量偏差，可继续像素级微调

## Iteration 4（首页主内容：背景色观感 + 车票查询表单排版/字距）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_4.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异
  - **本轮重点**：背景色观感（轮播区）与查询表单（卡片内部排版/间距）

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **位置/尺寸**：查询卡片顶部 tabs 区域在目标图更紧凑（高度更小、分割线更贴近 tabs），复刻图 tabs 行偏高、下划线位置偏下 - *查询卡片顶部 tabs* - **位置/尺寸**
  2. **位置/尺寸**：出发地/到达地输入区整体在复刻图中偏“下移”，导致日期/复选框/按钮整体下移 - *查询卡片表单区域* - **位置/尺寸**
  3. **位置/尺寸**：出发地/到达地右侧的“切换”橙色圆形按钮在目标图位于两行输入中间；复刻图位置偏下（更靠近日期行） - *出发地/到达地右侧* - **位置/尺寸**
  4. **位置/尺寸**：日期行与上方到达地输入行之间在目标图存在明显间距（不是紧贴），复刻图间距偏小 - *出发日期输入行* - **位置/尺寸**
  5. **位置/尺寸**：学生/高铁动车两项在目标图更“松”（两项间距更大、行高更高），复刻图更“挤” - *复选框区域* - **位置/尺寸**
  6. **其他（文本/字距）**：目标图“查 询”按钮文字中间字距更大（`查    询`），复刻图字距偏小 - *查询按钮* - **其他**

- **从原站提取的信息（用于修复）**：
  - **查询卡片内容区（原站 `.search-main-item`）**：padding `32px 25px 0`
  - **顶部 tabs 容器（原站 `.search-tab-hd`）**：高度 `34px`；分割线 `2px solid #DEDEDE`
  - **tabs 单项（原站每项约 `90×32`）**：字体 `14px`；选中态文字颜色 `#3B99FC`
  - **切换按钮（原站 `#danChange`）**：`24×24`；背景 `#FF8000`；白边 `2px`；圆形（`border-radius: 100%`）；位于两行输入中间
  - **复选框行**：行高约 `30px`；两项间距约 `60px`
  - **查询按钮文字**：`查&nbsp;&nbsp;&nbsp;&nbsp;询`

- **修复内容**：
  - **修改文件**：
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.tsx`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1：压缩 tabs 区域（高度/分割线/按钮尺寸），并调整选中下划线位置与厚度
    - #2：调整卡片内容区 padding 与表单行间距，使输入/日期/复选/按钮整体与目标更一致
    - #3：将“切换”按钮定位到两行输入的垂直中间
    - #4：补齐到达地与出发日期之间的间距（`date-row` 增加上边距）
    - #5：复选框行高与两项间距调整为更接近原站（`gap: 60px`、`height: 30px`），并将复选框改为更接近原站的直角小方框
    - #6：查询按钮文字改为 `查    询`（使用 NBSP），匹配原站字距观感

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_4_verify.png`
  - **结论**：查询卡片 tabs/输入/切换按钮/日期/复选框/按钮字距的排版已明显更接近目标图与原站测量值；轮播背景图片内容差异按要求忽略

## Iteration 5（首页主内容：页面背景色（应为白色）+ 查询表单图标/多余元素）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_5.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异
  - **本轮重点**：页面背景色（白）与查询表单（图标、控件是否多余、输入框细节）

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **颜色不一致**：目标图轮播区下方主内容背景为白色；复刻图主内容背景为浅灰（影响快捷服务区/推广区/信息区整体观感） - *轮播区下方主内容背景* - **颜色**
  2. **多余元素**：复刻图查询卡片右侧出现额外橙色圆形“收起”装饰点；目标图同位置没有该额外圆点 - *查询卡片右侧边缘* - **多余元素**
  3. **样式不一致**：目标图出发地/到达地输入框右侧为灰色定位图标（在输入框内部、约 30×30）；复刻图为蓝色小下拉箭头（尺寸更小且样式不同） - *出发地/到达地输入框右侧* - **颜色/样式**
  4. **位置/尺寸不一致**：目标图出发日期输入框右侧日历图标在输入框内部（灰色、约 30×30）；复刻图日历图标在输入框外侧（偏右、蓝色小图标） - *出发日期输入框右侧* - **位置/尺寸**
  5. **布局不一致**：由于日历图标外置，复刻图出发日期输入区域整体宽度偏大、与其他输入框右边界不齐；目标图三行输入右侧对齐更一致 - *出发日期行* - **位置/尺寸**

- **从原站提取的信息（用于修复）**：
  - **页面背景**：`body { background-color: rgb(255, 255, 255) }`
  - **城市输入框**：`265×30`；padding `4px 30px 4px 10px`；border `1px solid rgb(222,222,222)`（#DEDEDE）；`border-radius: 0`
  - **城市输入右侧图标**：`.icon-place`，`30×30`，颜色 `rgb(220,220,220)`（#DCDCDC），absolute 定位 `right: 0; top: 0`
  - **日期输入框**：`265×30`；padding `4px 10px`；border `1px solid #DEDEDE`；`border-radius: 0`
  - **日期输入右侧图标**：`.icon-date`，`30×30`，颜色 `#DCDCDC`，absolute 定位 `right: 0; top: 0`

- **修复内容**：
  - **修改文件**：
    - `frontend/src/pages/HomePage.tsx`
    - `frontend/src/pages/HomePage.css`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.tsx`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1：将首页主内容背景由 `#F5F5F5` 调整为 `#FFFFFF`（快捷服务/推广/信息区容器背景同步变白）
    - #2：移除查询卡片右侧多余的“收起”橙色装饰圆点
    - #3：将城市输入右侧图标改为灰色定位图标（30×30，输入框内部右侧）
    - #4/#5：将日历图标移入日期输入框内部右侧（30×30 灰色），并将日期行宽度恢复为 `265px` 对齐其他输入

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_5_verify.png`
  - **结论**：主内容背景已从灰底改为白底；查询卡片多余圆点已移除；城市/日期输入右侧图标位置与颜色更接近目标图与原站计算样式；轮播背景图片内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - 查询卡片在轮播背景上的“投影/边缘过渡”仍可能与目标图存在轻微观感差异（可在下一轮继续微调）

## Iteration 6（导航栏选中态 + 查询表单 tabs 视觉对齐）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_6.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/底部栏不调整；忽略轮播导致的背景图片内容差异（仅关注查询表单卡片与导航“选中态”）

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **样式缺失**：目标图导航栏“首页”是明显选中态（更深蓝色背景块），复刻图“首页”与其他项一致无选中态 - *导航栏 / 首页* - **缺失元素/样式**
  2. **颜色不一致**：目标图 tabs 未选中项文字为浅灰（更接近 `#999`），复刻图未选中项为深灰（更接近 `#333`） - *查询卡片 / tabs 行* - **颜色**
  3. **尺寸不一致**：目标图 tabs 左侧图标为 `16×16`，复刻图图标偏小（视觉上不够醒目） - *查询卡片 / tabs 行图标* - **位置/尺寸**
  4. **间距不一致**：目标图 tabs 图标与文字间距约 `5px`，复刻图间距偏小（更“挤”） - *查询卡片 / tabs 行图标与文字之间* - **位置/尺寸**
  5. **排版不一致**：目标图 tabs 文本在 `32px` 高度内垂直居中（line-height 与高度一致），复刻图 tabs 的 line-height 非 32px 导致视觉居中略偏 - *查询卡片 / tabs 行* - **位置/尺寸**

- **从原站提取的信息（用于修复）**：
  - **导航“首页”选中态**：
    - `.nav-hd`（首页 link）背景：`rgb(38, 118, 227)`（`#2676E3`）
    - 尺寸：`145×40`；`line-height: 40px`
  - **查询 tabs（单程/往返/中转换乘/退改签）**：
    - tabs 高度：`32px`，`line-height: 32px`
    - 选中颜色：`rgb(59,153,252)`（`#3B99FC`）
    - 未选中颜色：`rgb(153,153,153)`（`#999999`）
    - 图标：`16×16`，右间距 `5px`

- **修复内容**：
  - **修改文件**：
    - `frontend/src/components/SecondaryNav/SecondaryNav.tsx`
    - `frontend/src/components/SecondaryNav/SecondaryNav.css`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1：为 `SecondaryNav` 增加基于路由 `location.pathname` 的选中态 class（`active`），并将选中背景设为 `#2676E3`
    - #2/#5：将 tabs 默认文字颜色改为 `#999999`，并将 `line-height` 调整为 `32px`
    - #3/#4：将 tabs 图标调整为 `16×16`，并设置 `margin-right: 5px`（取消 `gap` 造成的偏差）

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_6_verify.png`
  - **结论**：导航“首页”选中态与 tabs 行的颜色/图标尺寸/间距/垂直居中已对齐原站测量值；轮播背景图片内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - 查询卡片与轮播背景的阴影/边缘过渡仍可能存在轻微观感差异（需要继续像素级微调时再处理）

## Iteration 7（查询表单 icon 与尺寸对齐：出发地/到达地/出发日期/切换）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_7.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异（仅关注查询表单 icon 与尺寸）

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **样式不一致**：目标图“出发地”输入框右侧为 iconfont 定位图标（浅灰），复刻图为 SVG/遮罩实现，图标细节（线条/比例）不一致 - *查询卡片 / 出发地输入框右侧* - **其他（图标样式）**
  2. **样式不一致**：目标图“到达地”输入框右侧定位图标同上，复刻图图标细节不一致 - *查询卡片 / 到达地输入框右侧* - **其他（图标样式）**
  3. **样式不一致**：目标图“出发日期”输入框右侧为 iconfont 日历图标（浅灰），复刻图为 SVG/遮罩实现，图标形状不一致 - *查询卡片 / 出发日期输入框右侧* - **其他（图标样式）**
  4. **样式/尺寸不一致**：目标图“切换”按钮内部为 iconfont 箭头 glyph（白色，`16px`，`line-height: 20px`），复刻图为 SVG/遮罩实现，箭头形状与占比不一致 - *查询卡片 / 出发地与到达地右侧切换按钮* - **位置/尺寸**
  5. **渲染观感不一致**：目标图 iconfont 的抗锯齿与笔画粗细一致；复刻图使用 SVG/遮罩时笔画粗细与边缘过渡略不同 - *查询卡片 / 所有输入框右侧 icon* - **其他（渲染）**

- **从原站提取的信息（用于修复）**：
  - **iconfont 资源**：`https://www.12306.cn/index/fonts/iconfont.css`（woff2/woff/ttf）
  - **定位图标（place）**：
    - wrapper：`30×30`
    - `font-size: 18px`
    - `color: rgb(220,220,220)`（#DCDCDC）
  - **日历图标（date）**：
    - wrapper：`30×30`
    - `font-size: 18px`
    - `color: rgb(220,220,220)`（#DCDCDC）
  - **切换图标（qiehuan）**：
    - 图标圆：`24×24`，`background: rgb(255,128,0)`（#FF8000），`border: 2px solid #fff`
    - glyph：`font-size: 16px`，`line-height: 20px`，`color: #fff`

- **修复内容**：
  - **修改文件**：
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `frontend/public/fonts/home/iconfont.css`
    - `frontend/public/fonts/home/iconfont.woff2`
    - `frontend/public/fonts/home/iconfont.woff`
    - `frontend/public/fonts/home/iconfont.ttf`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1/#2/#3/#4/#5：将查询表单右侧 icon 从 SVG/遮罩改为原站同款 iconfont glyph（`place/date/qiehuan`），并按原站测量值设置尺寸与颜色（30×30/18px、24×24/16px/line-height 20px）

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_7_verify.png`
  - **结论**：查询表单（出发地/到达地/日期/切换）右侧 icon 的图形与尺寸已与目标图/原站 iconfont 对齐；轮播背景图片内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - `target_image.png` 的日期为 `2026-01-17`，复刻页当前为 `2026-01-18`（属于动态值导致的视觉差异；若需要与目标图像素级一致，可将默认日期固定为参考图日期）

## Iteration 8（查询表单 icon 对齐：左侧菜单 + 顶部“单程/往返/中转换乘/退改签”）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_8.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异（仅关注查询表单 icon 与标签一致性）

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **样式不一致**：目标图左侧菜单“车票”使用 iconfont（实心 glyph）图标；复刻图使用自绘 SVG/遮罩，图标轮廓与笔画风格不一致 - *查询卡片 / 左侧蓝色菜单（车票）* - **其他（图标样式）**
  2. **样式不一致**：目标图左侧菜单“常用查询”图标为 iconfont glyph；复刻图为自绘 SVG/遮罩，形状细节不一致 - *查询卡片 / 左侧蓝色菜单（常用查询）* - **其他（图标样式）**
  3. **样式/尺寸不一致**：目标图左侧菜单“订餐”图标相对更小（视觉上低于其它两项的字号）；复刻图与其它图标同尺寸，观感不一致 - *查询卡片 / 左侧蓝色菜单（订餐）* - **位置/尺寸**
  4. **样式不一致**：目标图顶部“单程/往返/中转换乘/退改签”标签前均为 iconfont glyph；复刻图为自绘 SVG/遮罩，图标形状不一致 - *查询卡片 / 顶部标签行* - **其他（图标样式）**
  5. **间距/对齐不一致**：目标图顶部标签的 icon 与文字间距为固定小间距（约 5px），icon 尺寸为 16px；复刻图的 icon 与文字间距/尺寸与原站不一致 - *查询卡片 / 顶部标签行* - **位置/尺寸**

- **从原站提取的信息（用于修复）**：
  - **icon 类型**：非图片，均为 iconfont（`font-family: icon`）+ `<i class="icon icon-xxx">`
  - **顶部标签 icon**：
    - `icon-dancheng/icon-wangfan/icon-huancheng/icon-chepiao`
    - `font-size: 16px`，与文字间距约 `5px`，未选中为 `#999999`，选中为 `#3B99FC`
  - **左侧菜单 icon**：
    - `icon-huochepiao/icon-cycx`：`font-size: 22px`
    - `icon-dingcan`：`font-size: 18px`（略小）

- **修复内容**：
  - **修改文件**：
    - `frontend/src/index.css`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.tsx`
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1/#2/#3：左侧菜单 icon 切换为原站 iconfont（`icon-huochepiao/icon-cycx/icon-dingcan`），并按原站字号（22px/18px）调整
    - #4/#5：顶部标签 icon 切换为原站 iconfont（`icon-dancheng/icon-wangfan/icon-huancheng/icon-chepiao`），并按原站 `16px` + `margin-right: 5px` 对齐
    - 全局引入 `/fonts/home/iconfont.css`，确保 `.icon` 与 `icon-xxx` glyph 正常渲染

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_8_verify.png`
  - **结论**：查询表单左侧菜单与顶部标签的 icon 已改为与原站一致的 iconfont glyph，尺寸与间距同步对齐；轮播背景图片内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - 若需要与 `target_image.png` 像素级一致：`target_image.png` 的日期为 `2026-01-17`，复刻页为 `2026-01-18`（动态值差异）

## Iteration 9（左侧菜单 icon-文字关系 + 友情链接/二维码区背景色）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_9.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏不调整；忽略轮播导致的背景图片内容差异

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **位置/布局不一致**：目标图左侧菜单（车票/常用查询/订餐）为「icon 在左、文字在右同一行」；复刻图为「icon 在上、文字在下」 - *查询卡片 / 左侧蓝色菜单* - **位置/尺寸**
  2. **对齐不一致**：目标图左侧菜单文字在每个 115px 高的条目内通过行高垂直居中；复刻图为上下居中分布，导致文字基线位置更靠下 - *查询卡片 / 左侧蓝色菜单（常用查询等）* - **位置/尺寸**
  3. **对齐不一致**：目标图左侧菜单 icon 靠左并占固定宽度（约 28px），文字自然从 icon 右侧开始；复刻图 icon 与文字均居中对齐，导致左侧留白与起始位置不同 - *查询卡片 / 左侧蓝色菜单（车票等）* - **位置/尺寸**
  4. **颜色不一致**：目标图“友情链接 + 二维码”所在区域底色为浅灰（#F8F8F8）；复刻图该区域底色为白色 - *页面底部 / 友情链接与二维码区域背景* - **颜色**
  5. **间距不一致**：目标图底部“友情链接 + 二维码”区域顶部留白更紧（约 20px）；复刻图顶部留白更大（约 30px） - *页面底部 / 友情链接与二维码区域外边距* - **位置/尺寸**

- **从原站提取的信息（用于修复）**：
  - **左侧菜单（以“常用查询”为例）**：
    - 元素类型：文字 + iconfont（非图片），icon 为 `<i>`（`float: left`）
    - 条目尺寸：`width: 100px; height: 115px; line-height: 115px`
    - icon：`width: 28px; font-size: 22px; line-height: 115px; text-align: center`
  - **底部区域背景**：
    - “友情链接/二维码”外层可见底色来自 `.footer`，为 `rgb(248, 248, 248)`（#F8F8F8）

- **修复内容**：
  - **修改文件**：
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `frontend/src/components/BottomNavigation/BottomNavigation.css`
    - `frontend/src/pages/HomePage.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1/#2/#3：将左侧菜单从 `flex-direction: column` 调整为原站布局（icon 左浮动 + 文字同行，按条目高度设置 `line-height` 实现垂直居中）
    - #4/#5：将“友情链接 + 二维码”区域背景色调整为 `#F8F8F8`，并将顶部 padding 调整为 `20px`；保持版权灰条背景不受影响

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_9_verify.png`
  - **结论**：左侧菜单 icon-文字关系已与目标图一致（同行且垂直居中）；友情链接/二维码区域底色已对齐为 `#F8F8F8`

- **剩余问题（下一轮处理）**：
  - 若继续像素级对齐：可再检查友情链接区的细微间距/边框对比（目前已对齐主要背景色与菜单布局）

## Iteration 10（查询表单：搜索容器内组件对齐/边框/切换按钮位置）
- **对比基准**：
  - `target_image.png`（来自 `https://www.12306.cn/index/` 的整页截图）
  - `replica_page_iteration_10_verify_v2.png`（来自 `http://localhost:5174/` 的整页截图）
  - **忽略项**：顶部栏/导航栏/底部栏不调整；忽略轮播导致的背景图片内容差异
  - **本轮重点**：仅关注查询卡片（搜索容器）内部组件的位置、对齐、边框等

- **差异点（至少 5 处，按优先级：缺失/多余 > 位置尺寸 > 颜色 > 其他）**：
  1. **位置不一致**：目标图右侧橙色“切换”圆按钮更贴近输入框右侧边缘并有覆盖关系；复刻图该按钮偏外侧（更像完全悬浮在卡片外） - *查询卡片 / 出发地&到达地右侧* - **位置/尺寸**
  2. **对齐不一致**：目标图输入框内 placeholder/文字呈现更垂直居中；复刻图文字视觉上略偏（行高不一致导致） - *查询卡片 / 出发地&到达地输入框* - **位置/尺寸**
  3. **对齐不一致**：目标图出发日期输入框内文字垂直居中且与其它输入行一致；复刻图日期行文字视觉居中略有偏差 - *查询卡片 / 出发日期输入框* - **位置/尺寸**
  4. **尺寸不一致**：目标图“学生/高铁/动车”后的方框更接近 16×16；复刻图方框尺寸观感略小 - *查询卡片 / 复选框行* - **位置/尺寸**
  5. **其他（动态值差异）**：目标图出发日期显示为 `2026-01-17`，复刻图显示为 `2026-01-18` - *查询卡片 / 出发日期文本* - **其他（文本）**

- **从原站提取的信息（用于修复）**：
  - **查询卡片容器**：
    - `.search-index`：`510×350`
    - 左侧菜单（`.search-side`）：`width: 100px; height: 350px`
    - 表单区（`.search-form`）：`width: 360px`
  - **标签与输入**：
    - label：`width: 75px; height: 30px; line-height: 30px; padding-right: 10px`
    - 城市输入（`#fromStationText`）：`265×30`；`padding: 4px 30px 4px 10px`；`line-height: 20px`；`border: 1px solid #DEDEDE`
    - 日期输入（`#train_date`）：`265×30`；`padding: 4px 10px`；`line-height: 20px`；`border: 1px solid #DEDEDE`
  - **切换按钮**：
    - 参考结构：`.city-change` + `.icon-qiehuan`
    - 视觉：橙色圆点靠近输入右侧并覆盖（需要 `z-index`）
  - **复选框**：
    - 原站使用 16px 勾选资源（`check.png`/`check@2x.png`），未选中为 16×16 白底灰边框

- **修复内容**：
  - **修改文件**：
    - `frontend/src/components/TrainSearchForm/TrainSearchForm.css`
    - `iteration_log.md`
  - **已修复差异点**：
    - #1：将 `.swap-button-center` 的定位向内收（更贴近输入框右侧）并添加 `z-index`，形成更接近目标图的覆盖关系
    - #2/#3：为城市输入与日期输入补齐 `line-height: 20px`，使文字/placeholder 更垂直居中
    - #4：将复选框尺寸调整为 `16×16`，贴近原站观感

- **验证结果**：
  - 复刻页最新截图：`replica_page_iteration_10_verify_v2.png`
  - **结论**：搜索容器内部的“切换按钮覆盖关系/输入文字垂直居中/复选框尺寸”已更接近目标图；轮播背景内容差异按要求忽略

- **剩余问题（下一轮处理）**：
  - 若需要与 `target_image.png` 像素级一致：日期文本属于动态值（目标为 `2026-01-17`），可考虑在纯 UI 复刻模式下固定默认日期